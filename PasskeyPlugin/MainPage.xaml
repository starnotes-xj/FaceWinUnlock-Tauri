<Page
    x:Class="PasskeyManager.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:PasskeyManager"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <local:BitwiseFlagToVisibilityConverter x:Key="BitwiseFlagToVisibilityConverter" />
                    <SolidColorBrush x:Key="SystemFillColorSuccessBrush" Color="{ThemeResource SystemFillColorSuccess}" />
                    <SolidColorBrush x:Key="SystemFillColorCriticalBrush" Color="{ThemeResource SystemFillColorCritical}" />
                    <SolidColorBrush x:Key="SystemFillColorCautionBrush" Color="{ThemeResource SystemFillColorCaution}" />
                    <SolidColorBrush x:Key="SystemFillColorNeutralBrush" Color="{ThemeResource SystemFillColorNeutral}" />
                    <Style x:Key="CardStyle" TargetType="Grid">
                        <Style.Setters>
                            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
                            <Setter Property="BorderThickness" Value="1" />
                            <Setter Property="Padding" Value="12" />
                            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                            <Setter Property="CornerRadius" Value="{StaticResource OverlayCornerRadius}" />
                        </Style.Setters>
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid
            Padding="0,0,160,0"
            ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Image
                    Width="16"
                    Height="16"
                    Margin="16,0,8,0"
                    Source="ms-appx:///Assets/icon.ico" />
                <TextBlock
                    x:Name="headerTitleTextBlock"
                    VerticalAlignment="Center"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="FaceWinUnlock 通行密钥" />
            </StackPanel>
            <TextBlock
                x:Name="languageLabelTextBlock"
                Grid.Column="1"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="语言" />
            <ComboBox
                x:Name="languageSelector"
                Grid.Column="2"
                Width="104"
                Margin="0,8,0,8"
                SelectedIndex="0"
                SelectionChanged="languageSelector_SelectionChanged">
                <ComboBoxItem Content="中文" />
                <ComboBoxItem Content="English" />
            </ComboBox>
        </Grid>
        <Grid
            Grid.Row="1"
            Padding="16,24,16,16"
            ColumnSpacing="24"
            RowSpacing="8"
            Visibility="Visible">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="pluginSectionTitle" Style="{StaticResource BodyStrongTextBlockStyle}" Text="插件" />
            <Grid
                Grid.Row="1"
                Grid.ColumnSpan="2"
                Padding="12"
                Style="{StaticResource CardStyle}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="pluginStateTextBlock" VerticalAlignment="Center">
                    <Run x:Name="pluginStateLabelRun" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="状态：" />
                    <Run
                        x:Name="pluginStateRun"
                        Foreground="{ThemeResource SystemFillColorCautionBrush}"
                        Text="" />
                </TextBlock>
                <StackPanel
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button
                        x:Name="registerPluginButton"
                        Click="registerPluginButton_Click"
                        Content="注册"
                        Style="{StaticResource AccentButtonStyle}" />
                    <Button
                        x:Name="updatePluginButton"
                        Click="updatePluginButton_Click"
                        Content="更新"
                        Style="{StaticResource AccentButtonStyle}" />
                    <Button
                        x:Name="activatePluginButton"
                        Click="activatePluginButton_Click"
                        Content="启用"
                        Style="{StaticResource AccentButtonStyle}" />
                    <Button
                        x:Name="unregisterPluginButton"
                        Click="unregisterPluginButton_Click"
                        Content="移除" />
                </StackPanel>
            </Grid>


            <TextBlock
                x:Name="statsSectionTitle"
                Grid.Row="2"
                Margin="0,16,0,0"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="统计" />
            <Grid Grid.Row="3" Style="{StaticResource CardStyle}">
                <InfoBarPanel>
                    <TextBlock x:Name="credentialStateTextBlock1">
                        <Run x:Name="localDbLabelRun" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="本地库：" />
                        <Run x:Name="credsStatsRun1" Text="未加载" />
                        <LineBreak /><Run x:Name="windowsCacheLabelRun" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Windows 缓存：" />
                        <Run x:Name="credsStatsRun2" Text="未加载" />
                    </TextBlock>
                </InfoBarPanel>
            </Grid>
            <TextBlock
                x:Name="configurationSectionTitle"
                Grid.Row="2"
                Grid.Column="1"
                Margin="0,16,0,0"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="配置" />
            <Grid
                Grid.Row="3"
                Grid.Column="1"
                Style="{StaticResource CardStyle}">

                <StackPanel
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="8">
                    <ToggleSplitButton
                        x:Name="VaultUnlockControl"
                        Content="密码库已解锁"
                        IsCheckedChanged="VaultUnlockControl_IsCheckedChanged">
                        <ToggleSplitButton.Flyout>
                            <Flyout Placement="Bottom">
                                <ToggleSwitch
                                    x:Name="vaultLockSwitch"
                                    Header="解锁方式"
                                    IsOn="False"
                                    OffContent="确认"
                                    OnContent="通行密钥"
                                    Toggled="vaultLockSwitch_Toggled">
                                    <ToggleSwitch.HeaderTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                Margin="0,0,0,0"
                                                Padding="0,0,0,0"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Text="{Binding}" />
                                        </DataTemplate>
                                    </ToggleSwitch.HeaderTemplate>
                                </ToggleSwitch>
                            </Flyout>
                        </ToggleSplitButton.Flyout>
                    </ToggleSplitButton>
                    <ToggleSwitch
                        x:Name="silentOperationSwitch"
                        Header="最小化界面"
                        IsOn="False"
                        OffContent="显示"
                        OnContent="隐藏"
                        Toggled="silentOperationSwitch_Toggled">
                        <ToggleSwitch.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock
                                    Margin="0,0,0,0"
                                    Padding="0,0,0,0"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{Binding}" />
                            </DataTemplate>
                        </ToggleSwitch.HeaderTemplate>
                    </ToggleSwitch>
                </StackPanel>
            </Grid>


            <TextBlock
                x:Name="credentialsSectionTitle"
                Grid.Row="4"
                Margin="0,16,0,0"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="凭据" />
            <StackPanel
                Grid.Row="5"
                Orientation="Horizontal"
                Spacing="8">
                <Button
                    x:Name="refreshButton"
                    Click="refreshButton_Click"
                    Content="刷新"
                    Style="{StaticResource AccentButtonStyle}" />
                <DropDownButton x:Name="addCredentialsButton" Content="添加">
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem
                                x:Name="addAllPluginCredentialsMenuItem"
                                Click="addAllPluginCredentials_Click"
                                Text="全部通行密钥写入缓存" />
                            <MenuFlyoutItem
                                x:Name="selectedAddButton"
                                Click="addSelectedCredentials_Click"
                                IsEnabled="False"
                                Text="所选通行密钥写入缓存" />
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
                <DropDownButton x:Name="deleteCredentialsButton" Content="删除">
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem
                                x:Name="deleteAllPluginCredentialsMenuItem"
                                Click="deleteAllPluginCredentials_Click"
                                Text="清空缓存中的全部通行密钥" />
                            <MenuFlyoutItem
                                x:Name="deleteSelectedCacheButton"
                                Click="deleteSelectedPluginCredentials_Click"
                                Text="删除缓存中的所选通行密钥" />
                            <MenuFlyoutItem
                                x:Name="deleteSelectedLocalButton"
                                Click="deleteSelectedPluginCredentialsEverywhere_Click"
                                Text="删除所选通行密钥（全部位置）" />
                            <MenuFlyoutItem
                                x:Name="deleteAllLocalCredentialsMenuItem"
                                Click="deleteAllLocalCredentials_Click"
                                Text="清空本地库中的全部通行密钥" />
                            <MenuFlyoutItem
                                x:Name="deleteAllCredentialsMenuItem"
                                Click="deleteAllCredentials_Click"
                                Text="清空全部位置的通行密钥" />
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </StackPanel>
            <Grid
                Grid.Row="6"
                VerticalAlignment="Stretch"
                Style="{StaticResource CardStyle}">
                <ListView
                    x:Name="credentialListView"
                    IsItemClickEnabled="False"
                    ItemsSource="{x:Bind CredentialList.credentials}"
                    SelectionChanged="SelectionChanged"
                    SelectionMode="Multiple">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:Credential">
                            <Grid Padding="8" ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="28" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <FontIcon
                                    Grid.RowSpan="2"
                                    Grid.Column="0"
                                    FontSize="16"
                                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                    Glyph="&#xF427;" />
                                <TextBlock Grid.Column="1" Text="{x:Bind UserName}" />
                                <TextBlock
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="{x:Bind RpName}" />
                                <Border
                                    Grid.RowSpan="2"
                                    Grid.Column="2"
                                    Padding="4"
                                    VerticalAlignment="Center"
                                    Background="{ThemeResource ControlAltFillColorSecondaryBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource ControlCornerRadius}"
                                    Visibility="{x:Bind CredentialOptions, Converter={StaticResource BitwiseFlagToVisibilityConverter}, ConverterParameter=AutofillCapable}">
                                    <TextBlock FontSize="12" Text="自动填充 / Autofill" />
                                </Border>
                                <Border
                                    Grid.RowSpan="2"
                                    Grid.Column="3"
                                    Padding="4"
                                    VerticalAlignment="Center"
                                    Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource ControlCornerRadius}"
                                    Visibility="{x:Bind CredentialOptions, Converter={StaticResource BitwiseFlagToVisibilityConverter}, ConverterParameter=MetadataStale}">
                                    <TextBlock
                                        FontSize="12"
                                        Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                        Text="过期 / Stale" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <TextBlock
                x:Name="logsSectionTitle"
                Grid.Row="4"
                Grid.Column="1"
                Margin="0,16,0,0"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="日志" />
            <Grid
                Grid.Row="6"
                Grid.Column="1"
                VerticalAlignment="Stretch"
                Style="{StaticResource CardStyle}">
                <ScrollViewer x:Name="statusScrollArea" VerticalScrollBarVisibility="Auto">
                    <RichTextBlock x:Name="statusTextBlock" FontFamily="Cascadia Code">
                        <Paragraph x:Name="textContent" FontSize="12" />
                    </RichTextBlock>
                </ScrollViewer>
            </Grid>

            <StackPanel
                Grid.Row="5"
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="8">
                <Button
                    x:Name="clearLogsButton"
                    Click="clearLogsButton_Click"
                    Content="清空" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
