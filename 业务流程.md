# FaceWinUnlock 业务流程

## 场景路由

| 场景 | 策略 | 说明 |
|------|------|------|
| Windows 登录 / 解锁 | **纯密码** | 密码（Approach B）秒过、最快；完全不碰 PIN |
| UAC 提权 / Chrome/Edge 查看或填充密码 (CredUI) | **人脸提交密码 → 失败回退原生 PIN** | WebAuthn 监视器健康且确认不在 Passkey 事务中时才允许浏览器 Unknown 兜底 |
| **Passkey / 通行密钥** | **专用 Passkey 插件** | WebAuthn 活动是通用 Credential Provider 的强否决信号；专用插件通过独立命名管道人脸授权完成 FIDO2 操作 |

**架构约束**：通用 Credential Provider 只提交 Windows 凭据，不探测系统 UI，也不模拟键盘输入。禁止重新引入 UI Automation、控件枚举或自动 PIN 注入；无法安全分类或验证失败时必须交还 Windows 原生流程。

---

## 步骤 1：用户首次设置 PIN

```
UI (Vue 前端 → Tauri 后端)
  ↓
用户输入 Windows Hello PIN (4-6 位数字)
  ↓
encrypt_pin() Tauri 命令:
  - 查 SID (LookupAccountNameW)
  - DPAPI 加密 (CRYPTPROTECT_LOCAL_MACHINE + SID 派生 entropy)
  - 写入 database.db → pin_store 表
  ↓
✅ 存储完成
```

## 步骤 2：Chrome / Passkey 触发

```
浏览器触发 WebAuthn / 查看或填充密码
  ↓
credentialuibroker.exe 启动（Session 1，用户桌面）
  ↓
加载凭据提供程序:
  └── FaceWinUnlock_Tauri.dll (我们的 DLL) — 面容解锁磁贴
  ↓
WebAuthn 活动监视器先分类：
  ├── Passkey / security key / FIDO2 / 设置 PIN → 通用 Provider 返回 E_NOTIMPL
  │     └── FaceWinUnlock Passkey 插件走独立人脸授权管道
  └── 明确密码场景，或满足健康守卫的 Chrome/Edge Unknown → 发送 "prepare" → Unlock EXE
```

## 步骤 3：人脸识别

```
用户活动 → SetWindowsHookExW 捕获 → 管道 "run"
  ↓
Unlock EXE 摄像头 → OpenCV 人脸识别 → 匹配
```

## 步骤 4：凭据提交（统一只交密码，Approach B）

```
人脸匹配 → Unlock 立即发送 "username\0password\0domain\0"（不再加载/注入 PIN）
  ↓
DLL Creds 线程 → SharedCredentials → CredentialsChanged
  ↓
GetSerialization → CredPackAuthenticationBufferW（#126 CRED_PACK_PROTECTED_CREDENTIALS）
  ↓
· 登录/解锁/UAC/查看密码：密码被接受 → 认证 ✅（秒过）
· CredUI 被拒：ReportResult 失败（#102 清 is_ready/is_unlocked）
    → broker 超时/拒绝回退 → 隐藏本 Provider → 交还 Windows 原生 PIN（用户手输）
```

Passkey 插件所需的 PIN 设置、修改、删除与重建由专用插件流程管理，不进入通用 Credential Provider，也不通过键盘注入实现。

---

## 已验证技术事实

| 事实 | 证据 |
|------|------|
| NCryptSetProperty("SmartcardPin") 对 NGC **不生效** | RSA: NTE_SILENT_CONTEXT; FIDO: 0x80090003 |
| NCryptExportKey 全部 sz=0 | 5 种格式全失败，ExportPol 不可运行时改 |
| ngcksp.dll 是 RPC 客户端 | Ghidra: Ndr64AsyncClientCall → ngcsvc.dll |
| 不可离线解密 NGC 密钥 | IAT: NgcIsoCtnr.dll 无 BCryptSecretAgreement/KDF |

## 管道协议

```
MansonWindowsUnlockRustServer (DLL→Unlock):
  "prepare" — 心跳
  "run"     — 开始人脸识别

MansonWindowsUnlockRustUnlock (Unlock→DLL):
  "username\0password\0domain\0"     — 凭据提交 (Approach B，当前唯一路径)
```

## 当前策略（2026-06 起）

- [x] **登录/解锁：纯密码**（Unlock `handle_unlock_client` 凭据就绪即发，无 PIN 等待/注入 → 秒过）
- [x] **CredUI：WebAuthn 强否决 + 密码场景人脸**（无法安全分类、超时或凭据被拒时交还 Windows 原生 PIN）
- [x] **Passkey：专用插件**（通用 Provider 不介入，独立管道人脸授权）
- [x] UIA、控件探测、自动 PIN 注入和解锁动画运行代码已删除
- [x] broker teardown 有界且后台线程持有 DLL 生命周期引用

## 待验证（真机）

- [ ] 登录/解锁：人脸过后**密码秒登**，不再有盲打 PIN 的迟滞
- [ ] UAC / Chrome/Edge 查看和填充密码：人脸直接通过；不通过时回退原生 PIN 框
- [ ] Passkey 注册/登录及安全密钥：通用 Provider 摄像头不启动，专用插件正常授权
