# FaceWinUnlock 业务流程

## 场景路由

| 场景 | 策略 | 说明 |
|------|------|------|
| Windows 登录 / 解锁 | **纯密码** | 密码（Approach B）秒过、最快；完全不碰 PIN |
| UAC 提权 / Chrome 查看密码 (CredUI) | **密码优先 → 失败回退原生 PIN** | 先交密码；被拒则隐藏磁贴、交还 Windows 原生 PIN 由用户手输 |
| **Passkey / 通行密钥** | **磁贴不支持** | 需 FIDO2 断言；凭据提供程序交的是「用户名+密码」交互登录凭据，种类不对，无法完成 WebAuthn。要做须走 Phase 2（浏览器扩展 + 本地 FIDO 签名器） |

**设计依据（2026-06 调整）**：用户实测「人脸过后盲打 PIN」比直接交密码慢、体验差，且 PIN 注入（SendInput）曾诱发 `CredentialUIBroker.exe` 卡死。密码在登录/解锁/UAC/查看密码场景均可用且最快，故**统一只交密码**；CredUI 被拒时回退 Windows 原生 PIN（复用已有 #102 + broker 回退）。**PIN 自动注入链路已弃用。**

---

## 步骤 1：用户首次设置 PIN

```
UI (Vue 前端 → Tauri 后端)
  ↓
用户输入 Windows Hello PIN (4-6 位数字)
  ↓
encrypt_pin() Tauri 命令:
  - 查 SID (LookupAccountNameW)
  - DPAPI 加密 (CRYPTPROTECT_LOCAL_MACHINE + SID 派生 entropy)
  - 写入 database.db → pin_store 表
  ↓
✅ 存储完成
```

## 步骤 2：Chrome / Passkey 触发

```
浏览器触发 WebAuthn / 查看密码
  ↓
credentialuibroker.exe 启动（Session 1，用户桌面）
  ↓
加载凭据提供程序:
  └── FaceWinUnlock_Tauri.dll (我们的 DLL) — 面容解锁磁贴
  ↓
DLL SetUsageScenario → 发送 "prepare" → Unlock EXE
```

## 步骤 3：人脸识别

```
用户活动 → SetWindowsHookExW 捕获 → 管道 "run"
  ↓
Unlock EXE 摄像头 → OpenCV 人脸识别 → 匹配
```

## 步骤 4：凭据提交（统一只交密码，Approach B）

```
人脸匹配 → Unlock 立即发送 "username\0password\0domain\0"（不再加载/注入 PIN）
  ↓
DLL Creds 线程 → SharedCredentials → CredentialsChanged
  ↓
GetSerialization → CredPackAuthenticationBufferW（#126 CRED_PACK_PROTECTED_CREDENTIALS）
  ↓
· 登录/解锁/UAC/查看密码：密码被接受 → 认证 ✅（秒过）
· CredUI 被拒（passkey，或不收密码的应用）：ReportResult 失败（#102 清 is_ready/is_unlocked）
    → broker 超时/拒绝回退 → 隐藏本 Provider → 交还 Windows 原生 PIN（用户手输）
```

**已弃用 PIN 注入（SendInput）**：登录/解锁盲打 PIN 慢于密码、体验差，且曾诱发 broker 卡死。
DLL 端 `inject_pin_sendinput` 与 Unlock 端 `pin_to_inject` 已不再触发（Unlock 不再发 `inject_pin`），
保留为历史代码。`pin_store` 存储 + UI 录入组件保留（休眠），便于未来按需启用其它 PIN 策略。

---

## 已验证技术事实

| 事实 | 证据 |
|------|------|
| NCryptSetProperty("SmartcardPin") 对 NGC **不生效** | RSA: NTE_SILENT_CONTEXT; FIDO: 0x80090003 |
| NCryptExportKey 全部 sz=0 | 5 种格式全失败，ExportPol 不可运行时改 |
| SendInput 从 DLL 同进程注入 **可行** | `--uia-blind-inject` + 同进程管道验证通过 |
| EnumWindows 定位 PIN Edit **可行** | DLL 在 credentialuibroker.exe 内，可递归子窗口 |
| Passkey 必须用 PIN | 已实测，密码凭据不生效 |
| ngcksp.dll 是 RPC 客户端 | Ghidra: Ndr64AsyncClientCall → ngcsvc.dll |
| 不可离线解密 NGC 密钥 | IAT: NgcIsoCtnr.dll 无 BCryptSecretAgreement/KDF |

## 管道协议

```
MansonWindowsUnlockRustServer (DLL→Unlock):
  "prepare" — 心跳
  "run"     — 开始人脸识别

MansonWindowsUnlockRustUnlock (Unlock→DLL):
  "username\0password\0domain\0"     — 凭据提交 (Approach B，当前唯一路径)
  "inject_pin:XXXX"                 — PIN 注入 (Approach A，已弃用，Unlock 不再发送)
```

## 当前策略（2026-06 起）

- [x] **登录/解锁：纯密码**（Unlock `handle_unlock_client` 凭据就绪即发，无 PIN 等待/注入 → 秒过）
- [x] **CredUI：密码优先**（同一密码路径；被拒由 #102 + broker 回退交还 Windows 原生 PIN）
- [x] 弃用 PIN 注入（`main.rs` 移除 `pin_to_inject`/`pin_pending`；DLL 端 inject_pin 处理保留为休眠死代码）
- [x] broker teardown 卡死根治（动画 Drop 有界分离 + 后台线程 dll_add_ref 守护分离 + 钩子专用消息泵线程）
- [~] `pin_store` 存储 + Vue 录入组件保留休眠（当前策略不用，便于未来扩展）

## 待验证（真机）

- [ ] 登录/解锁：人脸过后**密码秒登**，不再有盲打 PIN 的迟滞
- [ ] UAC / Chrome 查看密码：密码直接通过；不通过时回退原生 PIN 框
- [ ] Passkey：磁贴交密码被拒 → 回退原生（用户用真实 passkey）；**全程不卡死**
