# 用「真实 Windows Hello PIN」解锁（NGC 解密链）方案

> 本方案取代此前的"应用自定义 PIN"方案（curried-petting-meadow.md）。用户明确：PIN 必须是**真正的 Windows Hello PIN**，不计难度与时间成本。

## Context（为什么 + 目标）

让用户在本项目的凭据提供程序磁贴上**输入真实的 Windows Hello PIN** 完成解锁/登录/UAC，使认证统一、现代化，并作为面容识别的并列备选（摄像头不可用/光线差时用 PIN）。

**核心研究结论（决定整个方案，均有来源）：**
- **本地账户的 Windows Hello "convenience PIN" 本质 = 一个被 PIN 解锁的保险箱，里面存着缓存的明文账户密码**。微软官方："Convenience PIN ... still uses a password for authentication. When the correct convenience PIN is provided, the password information is loaded from its cache and authenticates the user." mimikatz 作者 Benjamin Delpy 实证："本地账户配 Hello PIN，明文密码在 SYSTEM vault，**有没有 TPM 都一样**。"
- **第三方凭据提供程序无法触发系统自带的 NGC Credential Provider**（passkey/查看密码走 NGC/WebAuthn 子系统，不枚举第三方 CP）。**KERB_CERTIFICATE_LOGON 的 PIN 路径只对域/WHfB 证书信任有效，本地账户用不了**。
- **因此：我们以 SYSTEM 身份，用用户输入的真实 PIN 复刻 NGC 解密链，离线解出该账户的真实密码，再走项目现有的凭据打包/提交链路登录。** 这条路对本地账户 **TPM-or-not** 都成立。
- **参考实现**：DEF CON 32「Abusing Windows Hello Without a Severed Hand」(Ceri Coburn / Dirk-jan) 开源工具 [Shwmae](https://github.com/CCob/Shwmae)（C#）；逆向细节见 [Synacktiv WHFB cache flow](https://www.synacktiv.com/en/publications/whfb-and-entra-id-say-hello-to-your-new-cache-flow)。

**正当性**：用户自有机器、自维护的开源解锁工具、用自己的 PIN 解锁自己的 Windows——与 KeePassWinHello 等集成 Hello 的合法工具同类，属授权自用。

## 已确认范围（用户拍板）

两个 Phase **都要实现**（用户明确 passkey「必须尝试」）：

- **Phase 1**：锁屏登录 / 解锁 / UAC 提权，用真实 Hello PIN（NGC 解密出真实密码再经 CP 提交）。
- **Phase 2**：passkey **自接管**——浏览器扩展拦截 `navigator.credentials.get` → 本地 SYSTEM FIDO 签名器用 PIN 解出的 Hello FIDO2 私钥签 assertion。

**先做 Phase 1 打通 NGC 解密底座，再做 Phase 2**（Phase 2 复用 Phase 1 的 NGC 模块解 FIDO 私钥）。

**机制必须说清（避免误解）**：研究已证实**无法让 broker/系统原生 passkey 弹窗改用我们的 PIN**——broker 不枚举第三方 CP，替换 `webauthn.dll` 受 WRP/TrustedInstaller 保护（禁 Secure Boot 也绕不过）。Phase 2 的唯一现实形态就是 Shwmae 路线：**浏览器扩展 + 本地签名器**，在浏览器层接管，不走系统原生认证器。这与原生 Hello 在浏览器里的能力**功能重复**，且随浏览器/系统更新易碎——但能达成"绕开 broker 的 passkey/查看密码区分、统一用 PIN"的目标。

## 解密链（要移植的算法，已锁定细节）

终点：本地账户明文密码。以 SYSTEM 身份执行：

1. **定位 NGC 容器**：`C:\Windows\ServiceProfiles\LocalService\AppData\Local\Microsoft\Ngc\<GUID>\`，按目标用户 SID 找其容器与 **PIN protector**。
2. **PIN→熵**（参照 Shwmae `Ngc/NgcPin.cs`）：PIN→ASCII→大写十六进制串→UTF-16；`PBKDF2-HMAC-SHA256(该串, salt, rounds)`（salt/rounds 取自 protector 文件）→ 32 字节 → 十六进制 → UTF-16 → `SHA512` → 前置固定熵 `"xT5rZW5qVVbrvpuA\0"`。
3. **解 NGC RSA 私钥**：用上一步的熵作 DPAPI entropy，解 `%WINDIR%\ServiceProfiles\LocalService\AppData\Roaming\Microsoft\Crypto\Keys` 下的 RSA 私钥 blob。
4. **解 vault**（参照 Shwmae `Vault/Policy.cs` + `Vault/Credential.cs`）：解 vault policy(.pol) 得 `Aes256Key`；用它 AES 解 NGC `.vcrd`（schema `1d4350a3-330d-4af9-b3ff-a927a45998ac`）得 `NgcCredential{EncData, IV, EncPassword}`。
5. **解出明文密码**：用 NGC RSA 私钥解 `EncData` 得对称密钥 → AES 解 `EncPassword` → **明文账户密码**。
6. **PIN 错误**：第 2/3 步解密失败即判定 PIN 错误，拒绝。

**关键简化**：Unlock.exe 是**活体 SYSTEM 进程**，第 3 步直接调 Win32 `NCryptUnprotectSecret`(DPAPI-NG) / `CryptUnprotectData`（SYSTEM 上下文，附 PIN 派生 entropy），第 4/5 步用 CNG（`BCrypt*`）做 AES/RSA——**无需像 Shwmae 那样离线重写 DPAPI masterkey/hive 解析**，移植量大幅缩小。仅需自己实现：NGC/vault 文件格式解析 + PIN 熵派生（PBKDF2+SHA512+固定熵）。

## 架构（复用现有管道 + matched_creds，DLL 改动小）

```
磁贴输入真实 Hello PIN → 提交
  → DLL 经现有 PIPE_UNLOCK_NAME 发 "pin:<SID>:<PIN>"（同 release 通道）
  → DLL 返回 CPGSR_NO_CREDENTIAL_NOT_FINISHED（等异步结果），读回 ok/fail 显示
        │
Unlock.exe（SYSTEM，is_server=true 已是 SYSTEM）handle_unlock_client peek 分支
  → 解析 pin: → ngc::recover_password(sid, pin)   ← 新模块，跑上面的解密链
        ├─ 失败：回 "fail"
        └─ 成功：state.matched_creds = Some((user, 明文密码, domain))  ← 与面容匹配同一出口
        │
DLL Creds 线程收到凭据 → is_ready/is_unlocked → CredentialsChanged
  → autologon → 现有 GetSerialization + CredPackAuthenticationBufferW(#126) → 登录
```

**运行身份**：Unlock 后台服务以 `is_server=true` 创建计划任务 → 已是 `S-1-5-18`(SYSTEM)（见 `UI/src-tauri/src/utils/api.rs:282,297`）。NGC 容器、Crypto\Keys、SYSTEM vault 均在 LocalService/SYSTEM 配置下，SYSTEM 可读。**无需改运行身份**。

## 改动清单

### A. 新增 Rust NGC 解密模块 —— `Unlock/src/ngc/`（核心工作量）
按 Shwmae 结构移植（以公开 DEF CON 研究 + Synacktiv 为据，clean-room 重写，遵守 Shwmae 许可证）：
- `mod.rs`：`recover_password(sid: &str, pin: &str) -> Result<(user, password, domain)>`
- `container.rs`：枚举/定位 NGC 容器与 protector（对应 `NgcContainer.cs`）
- `pin.rs`：PIN 熵派生（对应 `NgcPin.cs`，含固定熵 `xT5rZW5qVVbrvpuA\0`）
- `protector.rs` / `seal.rs`：protector 解密、NGC 解封（对应 `Ngc/Protectors`、`NgcSeal.cs`）
- `vault.rs`：vault policy + .vcrd 解析与解密（对应 `Vault/Policy.cs`、`Vault/Credential.cs`）
- `dpapi.rs`：薄封装 Win32 `NCryptUnprotectSecret`/`CryptUnprotectData`（活体 SYSTEM，省去离线 DPAPI 重写）
- `Cargo.toml` 增依赖：优先 windows-rs 的 `Win32_Security_Cryptography`(CNG/DPAPI)；纯 Rust 仅补 `pbkdf2`+`hmac`+`sha2`（SHA256/512）、必要时 `aes`/`cbc`、`der`/ASN.1。能用 CNG 就不引纯 Rust crypto。
- **单元测试**：用一台已知 PIN 的测试机导出 NGC/vault/Keys 文件作离线测试向量，断言解出预期密码。

### B. Unlock EXE —— `Unlock/src/main.rs`
- `handle_unlock_client` 的 peek 命令分支新增 `pin:` 前缀：解析 `<SID>:<PIN>` → `ngc::recover_password` → 成功则写 `state.matched_creds`（复用面容出口，含 #103 空用户名拒绝、#104 域名）并回 `ok`，失败回 `fail`。
- 失败退避（防 PIN 暴力）；明文密码用后 zero 化。

### C. Server DLL（改动小，与旧 PIN 方案字段部分一致）
- `Server/src/CSampleProvider.rs`：字段 2→4，新增 index2 `CPFT_PASSWORD_TEXT`(PIN 框)、index3 `CPFT_SUBMIT_BUTTON`；`GetFieldDescriptorCount`/`GetFieldDescriptorAt` 同步；`GetCredentialCount` 保留 #112 指针初始化，PIN 启用时正常显示磁贴。
- `Server/src/CSampleCredential.rs`：加 `pin_buffer`；实现 `SetStringValue`/`GetStringValue`/`GetSubmitButtonValue`/`GetFieldState`(由 `PIN_ENABLED` 控制可见)；`GetSerialization` 增 PIN 分支——读 PIN→`send_pin_to_unlock`→返回 NOT_FINISHED（成功走现有 autologon 打包；失败显示"PIN 错误"）。保留 #102/#126。提交后 `SecureZeroMemory` 清 `pin_buffer`。
- `Server/src/CPipeListener.rs` / `Pipe.rs`：新增 `send_pin_to_unlock(sid, pin)->bool`，连 `PIPE_UNLOCK_NAME` 写 `pin:<SID>:<PIN>`、读回 `ok`/`fail`；`is_pin_enabled()` 读注册表 `PIN_ENABLED`。

### D. UI（Tauri 后端 + 前端）
- **不存储 PIN**：真实 Hello PIN 每次现场输入并用于解密，**不落盘、不哈希**（与旧方案根本区别）。也不再依赖 `faces.user_pwd`（密码每次从 NGC 现解）。
- 后端：新增 `set_hello_pin_enabled(enabled)` 经现有 `write_to_registry` 写 `PIN_ENABLED`；可加 `probe_ngc(sid)` 自检该用户是否启用了 Hello PIN（用于设置页提示）。确保部署时 Unlock 计划任务以 SYSTEM 创建（`is_server=true`）。
- 前端 `UI/src/views/Options.vue`：新增"Hello PIN 解锁"开关（写 `PIN_ENABLED`）+ 说明文案（需本地账户已设 Hello PIN）。`PIN_ENABLED` 纳入"同步至系统注册表"镜像（与 `SHOW_TILE`/`UNLOCK_SCENE` 并列）。

### E. 注册表新增键（`HKLM\SOFTWARE\facewinunlock-tauri`）
| 键            | 值          | 说明                                          |
| ------------- | ----------- | --------------------------------------------- |
| `PIN_ENABLED` | `"0"`/`"1"` | 默认 `"0"`；DLL 据此显示 PIN 字段。灰度开关。 |

## 安全 / 现实约束（如实记录）

- 采用 NGC 离线解密技术（Shwmae/DEF CON 32 同源）服务于**正当自用**：用户用自己的 PIN 解锁自己的机器。
- 解出的明文密码仅短暂驻留内存，用后 `SecureZeroMemory`；PIN 明文只在本地命名管道内短暂传输。
- **AV 误报**：NGC/vault 解密代码形似 mimikatz/Shwmae，杀软可能告警；缓解：用户自有签名构建、加排除项。
- **适用账户**：Phase 1 限**本地账户**（及密码被缓存的本地/AD convenience PIN），TPM-or-not 均可。**Microsoft/Entra 账户**走 CloudAP/PRT 不同缓存，本期不支持，需文档说明。
- **版本敏感**：NGC/vault 格式随 Windows 版本可能变化；以测试向量回归，失败时回退（仍可用面容/密码）。

## Phase 2：passkey 自接管（浏览器扩展 + 本地 SYSTEM FIDO 签名器）

复用 Phase 1 的 `ngc` 模块解出 **Hello FIDO2 平台凭据私钥**（NGC 容器里的 `FIDO_AUTHENTICATOR//...` 密钥），在浏览器层接管 passkey 登录断言。仅实现 `navigator.credentials.get`（登录断言，**不**碰需 attestation 的 `create`/注册——用户用已有 passkey）。

### 数据流
```
网站调用 navigator.credentials.get(challenge, rpId, allowCredentials)
  → 浏览器扩展 webauthn-inject.js 覆写 get，转发 {origin, options} 给扩展后台
  → 扩展 POST 到本地签名器 http://127.0.0.1:<port>（带鉴权 token）
  → 签名器(SYSTEM)：向用户索要 Hello PIN（弹窗）→ ngc 解 FIDO 私钥
      → 构造 authenticatorData(rpIdHash+Flags=0x05(UP|UV)+signCount)
      → 签名 (authenticatorData ‖ SHA256(clientDataJSON)) ，clientDataJSON={type:"webauthn.get",challenge,origin}
  → 返回 {id,rawId,authenticatorData,clientDataJSON,signature,userHandle}
  → 扩展回填给页面 → RP 验证通过
```

### 组件 / 文件（移植自 Shwmae，clean-room 重写）
- **本地 SYSTEM 签名器**（新二进制，建议 `FidoSigner/` 或并入 Unlock 的子命令）：
  - 复用 `Unlock/src/ngc`（解 FIDO 私钥，对应 Shwmae 枚举里的 `FIDO_AUTHENTICATOR` 键）
  - `fido2.rs`：assertion 构造（对应 `Fido2/AuthenticatorData.cs`：`Flags=0x05`、`SignCount` 大端、`RpIdHash=SHA256(rpId)`；`AuthenticatorAssertionResponse.cs`、`PublicKeyCredential.cs`）
  - `signer.rs`：用 CNG 以 FIDO 私钥签名（对应 `Signers/CngRSAAlgorithm.cs`，Windows 平台认证器多为 RSA）
  - `http.rs`：本地 HTTP API（对应 `HttpListener.cs`），**绑定 127.0.0.1 + 共享 token 鉴权**（Shwmae 原版无鉴权，是攻击工具；产品必须加，否则本机任意进程可滥用）
  - **signCount**：需按 credId 持久化并单调递增（RP 会校验），存 SQLite/文件
- **浏览器扩展**（新 `BrowserExt/`，改自 `ShwmaeExt`）：
  - `webauthn-inject.js`：页面 `document_start` 注入，覆写 `navigator.credentials.get`（参照已读源码：base64 编解码 ArrayBuffer、转发、回填 assertion 形状）
  - `content.js`/`background.js`/`manifest.json`：MV3 扩展骨架，转发到本地签名器；**用我们自己的扩展 ID**（不要 Shwmae 的硬编码 ID `ijacbjjjpmhencpkoghphdgbooifplmn`）
  - 需在 Chrome/Edge 各自安装（开发者模式或企业策略 force-install）

### Phase 2 独有难点（必须处理）
- **PIN 来源**：浏览器流程没有锁屏磁贴 → 签名器须自己弹窗向用户要 Hello PIN（每次断言或短时缓存 PIN 派生密钥，缓存有泄露风险，默认每次询问）。
- **本地端口鉴权**：扩展↔签名器必须用一次性/安装期 token，防止本机其他程序调用签名器伪造断言。
- **仅断言**：注册(`create`)需 attestation（AAGUID/证书），不实现；用户须先用系统原生 Hello 注册好 passkey。
- **冗余与易碎**：与原生 Hello 在浏览器的能力重复；浏览器升级改 WebAuthn 行为、或站点用 `create`/条件式 UI 时可能失效。灰度开关 `PASSKEY_TAKEOVER_ENABLED`（默认关）。

### Phase 2 验证
- 选一个支持 passkey 登录的站点（已用系统原生 Hello 注册过 passkey）。
- 装扩展 + 起签名器 → 站点点"用 passkey 登录" → 扩展拦截 → 输 Hello PIN → 断言通过、成功登录。
- 关扩展/关 `PASSKEY_TAKEOVER_ENABLED` → 回落到系统原生 Hello，行为如常。
- 验证 signCount 单调递增、错误 PIN 被拒、token 鉴权拦截非授权本地调用。

## 已修复 Issue 回归禁令（改动后逐项核对）
触及字段/凭据计数/序列化，重点复核：**#112**(GetCredentialCount 指针初始化 + is_unlocked 脉冲)、**#102**(ReportResult 失败清标志)、**#126**(CRED_PACK_PROTECTED_CREDENTIALS)、**#118**(SetUsageScenario 外场景 E_NOTIMPL)、**#91**(CPFT_SMALL_TEXT)、**#103/#104**(空用户名拒绝/域名)。

## 验证（end-to-end）

构建：仓库根 `cargo build --release`（环境变量见 CLAUDE.md）；UI 单独 build。

NGC 模块先行：用测试机导出的 NGC/vault/Keys 文件作离线测试向量，单测 `recover_password` 解出预期密码（含错误 PIN 应失败）。

VM 功能测试（**分别测 TPM 开启 与 TPM 关闭 两台**，本地账户 + 已设 Hello PIN）：
1. 设置页开启"Hello PIN 解锁"→ `PIN_ENABLED=1`，确认 Unlock 以 SYSTEM 运行。
2. **锁屏**：磁贴输真实 Hello PIN → 解出密码 → 秒登录；输错 PIN → 提示错误、可重试；遮挡摄像头时 PIN 仍可解锁。
3. **UAC**：提权弹窗 → 输 PIN → 通过。
4. **改密码后**：系统改账户密码（缓存随之更新）→ PIN 解锁仍登录成功。
5. **改 PIN 后**：改 Hello PIN → 新 PIN 可解、旧 PIN 失败。
6. **关闭开关**（`PIN_ENABLED=0`）：磁贴退化为纯面容，行为与今天一致。
7. **回归**：连续 100 次锁屏/解锁（面容 + PIN 交替），核对上方 Issue 清单无回归。

日志：`facewinunlock.log`（DLL，PID 区分）+ Unlock 服务日志中的 `pin:`/NGC 解密结果（**不得记录 PIN 或明文密码**）。

合并前置：VM 100 次连续验证通过方可并入主分支；灰度由 `PIN_ENABLED` 默认关闭保护存量环境。